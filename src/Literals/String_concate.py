# Concatenation between string is allowed, only the pblm is using incompatibility data type.

first_name = "Chitra"
last_name = "M"

full_name = first_name + " " + last_name
print(full_name)
print(type(full_name))